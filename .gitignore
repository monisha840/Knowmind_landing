node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Credential files dropped into the working tree (Razorpay key exports, etc.)
# never belong in history.
rzp-*.csv
*key*.csv

.vercel
.env*

# Source capture downloaded from the owner's Drive asset library for
# scripts/optimize-video.mjs. Half a gigabyte of raw phone video; the
# web-ready encodes in public/testimonials are what ship.
media-source/

# Scratch build output, so verifying a build cannot empty the directory a
# running dev server is serving from. See distDir in next.config.ts.
.next-verify/

# The VSL master — raw phone capture (2688x1512, ~16.5 Mbps, 142 MB), so unlike
# the file this replaced it is re-encoded rather than remuxed; `public/kalee/vsl.mp4`
# is the web-ready result and is what ships. Keep this file locally; nothing
# needs it to rebuild.
updated_video.MP4
VSL_video.mp4

# Generator-script inputs at the project root, same rule as the VSL master
# above: the optimized output they produce is already committed under
# public/, so the raw source only bloats history. Keep these files locally.
LP_corrections.pptx
updated_hero.png
